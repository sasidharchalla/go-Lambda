name: CI
on:
 push:
  branches: 
    - main
  #paths:
    #- backend/lambda/**
env:
  Deploy_Route_Url: ${{secrets.AWS_DEPLOY_DEV_ENV}}

jobs:
 build:
 
  runs-on: ubuntu-latest
  strategy: 
    matrix:
      go-version: [1.17]
  steps:
   - name: check the code
     uses: actions/Checkout@v2
     
   - name: test
     run: echo "tested"
  
   
   - name: Get changed files
     id: files
     uses: jitterbit/get-changed-files@v1
     with:
       format: 'csv'

   - name: Files in single string
     run: |
       printf '%s,' '${{ steps.files.outputs.added_modified }}'

   - name: Added files
     id: added-files
     run: |
       mapfile -d ',' -t added_modified_files < <(printf '%s,' '${{ steps.files.outputs.added_modified }}')
       echo "::set-output name=addfiles::added_modified_files"

  #  - name: setup go

  #    uses: actions/setup-go@v2
  #    with:
  #      go-version: ${{ matrix.go-version }}
  #    env:
  #     GO111MODULE: "on"
 
  #  - name: check setup
  #    run: |
     
  #      go mod init leapfrog-backend-Go-Lambdas
  #      go mod tidy
    
   - name: codebuld    
     run: |

         echo "Do something with this ${steps.added-files.outputs.addfiles[0]}."
       


 
     
